# 地理卡片库 · 网站部署说明

本目录是一个 **Quartz v5** 静态网站工程，已把 Obsidian 卡片库（八类核心卡片 + 入口/大纲/标签词典）转换为可发布的网站源码。**敏感/内部内容已默认剔除**（见末尾"内容边界"）。

---

## 一、本地预览（可选，验证用）

```bash
cd D:/geo-quartz
npx quartz build        # 生成 public/
npx quartz serve        # 本地 http://localhost:8080 预览
```
> 构建需要 Node 22 + 约 300MB 依赖（已装在 D 盘，避开 C 盘空间不足问题）。

---

## 二、推到 GitHub 并自动发布（推荐）

工程已内置 GitHub Actions 工作流（`.github/workflows/deploy.yml`）：**只要把源码推到 GitHub，每次 push 会自动构建并发布到 GitHub Pages**，你以后只改 `content/` 再 push 即可，不用本地构建。

### 步骤
1. 在 GitHub 新建一个**公开**仓库，例如 `geo-cards`（用户名假设 `yourname`）。
2. 在本机（D:/geo-quartz）初始化并提交：
   ```bash
   cd D:/geo-quartz
   git init -b main
   git remote add origin https://github.com/yourname/geo-cards.git
   git add -A
   git commit -m "init quartz site"
   git push -u origin main
   ```
3. 仓库 **Settings → Pages → Source** 选 `GitHub Actions`。
4. 等 Actions 跑完（约 1–2 分钟），访问 `https://yourname.github.io/geo-cards/`。

> 若用**用户页**（`yourname.github.io` 这个仓库名），地址就是 `https://yourname.github.io/`。
> 若用**项目页**（`geo-cards` 这种仓库），发布在 `https://yourname.github.io/geo-cards/`，需把 `quartz.config.yaml` 的 `baseUrl` 改成 `yourname.github.io/geo-cards`。

---

## 三、国内镜像：CloudStudio 部署（✅ 已上线）

GitHub Pages 在大陆偶尔不稳，已通过 **WorkBuddy CloudStudio** 部署国内镜像。

### 当前状态
- **国外站**：`https://rouxueqingge.github.io/geography-cards/`（GitHub Pages，自动构建）
- **国内站**：`https://c8f90232189c48bf994ebedb02911673.bj10.agentos-app.net/`（CloudStudio 北京节点 ✅）
- 管理入口：**「设置 - 数据管理 - 我发布的应用」**

### 部署原理
CloudStudio 只托管**已构建的静态文件**，不会 `npx quartz build`。
因此推送的是 `public/`（构建产物）的改写版本——域名已从 GitHub 改写（资源链接全是相对路径，无需重 build）。

### 一步到位流程（已跑通）
```bash
# 1) 把 public/ 里的 GitHub 地址改写成 Gitee 地址
python work/fix_gitee_paths.py   # 自动完成 + 删 CNAME + 加 .nojekyll

# 2) 拷到干净目录
cp -r public/. D:/gq-gitee/

# 3) 通过 WorkBuddy CloudStudio 工具一键部署 D:/gq-gitee/
```

### ⚠️ 关于 Gitee Pages（已停服）
> **Gitee Pages 已于 2025 年下架停服**（官方确认"功能已经下架"，2026 年完全不可用）。
> 最初方案是推送到 Gitee 仓库并启用 Pages，但该服务已不存在。
> Gitee 仓库 `geofans/geo-cards` 仍保留作为代码备份（公开），但无法用于静态站托管。
> 如需长期稳定的国内托管，建议后续迁移至**腾讯云云托管**或**阿里云 SAE**。

### 更新内容流程
1. 改 `content/` → 推 GitHub（Actions 自动重建 GitHub 版）
2. 对新 `public/` 重做路径改写 → 重新部署 CloudStudio
3. （可选）后续可做成 GitHub Action 同时双推，自动化两站同步

---

## 四、以后怎么更新内容

1. 直接编辑 `content/` 下对应卡片（保持原目录结构，相对链接才有效）。
2. `git push` → Actions 自动重建发布。
3. 想加回被剔除的内容（如某份教学设计），把它放进 `content/` 对应目录即可。

---

## 五、内容边界（重要）

进入网站的目录（白名单）：
`00_入口 · 01_大纲 · 02_知识卡 · 03_方法卡 · 04_图表卡 · 05_题目卡 · 06_错因纠偏卡 · 07_概念卡 · 08_句库 · 10_概念辨析卡 · 99_标签词典`

**默认未进入网站**（物理剔除，绝不会泄露）：
- `98_联动产出/`（含**命题资源**等内部产出）
- `09_课堂探究（教师）`（教师专用）
- `11_真题论文库`（评分细则等，先保守挡住）
- `_Archive` `待删除-重复卡`（归档/待删）
- `1-闪念笔记` `3-阅读笔记`（个人笔记）

如需公开其中某类，把对应文件夹拷进 `content/` 即可；**命题资源/高考命题组相关材料请务必保持私有**。

---

## 六、当前环境提示

- 构建依赖装在 **D 盘**（`D:/geo-quartz`、`D:/npm-cache`），因为 **C 盘已 100% 占满**，无法在 C 盘装依赖或构建。
- C 盘满的问题（Camtasia 录制 3.6GB、微信 1.47GB 等）建议尽快清理，否则后续本地操作都会受限。
